# T17: Make super-chat default in arxivite (was: "Fix arxivite — Replace ArxiviteLLMAdapter shim")

*Created: 2026-06-20 00:10:00 IST*
*Last Updated: 2026-06-20 00:32:00 IST*

**Status**: ⬜ **PENDING**
**Priority**: HIGH
**Phase**: Phase 2 — Fastest Win

## ❌ Correction from Code Audit (2026-06-20)

**The previous analysis was wrong.** `ArxiviteLLMAdapter` is NOT a broken regex shim. It is a **proper wrapper** around super-chat's `VercelLLMAdapter` with native Vercel AI SDK v6 tool calling.

**What actually exists:**
- arxivite has a `useSuperChat` feature flag in `chatbot.tsx` (default: `false`)
- When `useSuperChat=true`: Uses super-chat's `ChatEngine` with proper native tool calling via `ArxiviteLLMAdapter` (wrapper around `VercelLLMAdapter`)
- When `useSuperChat=false`: Uses legacy path with simulated tool calling (no streaming, fragile)
- arxivite already has proper adapters: `ArxiviteToolAdapter`, `ArxiviteRAGAdapter`, `ArxivitePersistenceAdapter`

## The Real Problem

The default is wrong. `useSuperChat=false` means arxivite uses the legacy path by default, which:
- Has no streaming
- Simulates tool calling (less reliable)
- Doesn't benefit from super-chat's improvements

## The Real Fix (much smaller effort)

1. **Change default**: `useState(false)` → `useState(true)` in `ChatbotAssistant.tsx`
2. **Remove legacy path**: Delete the `if (!useSuperChat)` branch entirely
3. **Keep super-chat path**: The `else` branch already works correctly
4. **Test**: Verify tool calling + RAG + streaming works end-to-end
5. **Polish**: Error handling, edge cases, loading states

## Files to Modify
- `arxivite/src/components/chat/ChatbotAssistant.tsx` — change default, remove legacy path

## Files to Keep (already work correctly)
- `arxivite/src/lib/super-chat/ArxiviteLLMAdapter.ts` — proper wrapper, NOT a shim
- `arxivite/src/lib/super-chat/ArxiviteToolAdapter.ts` — bridges tool registry
- `arxivite/src/lib/super-chat/ArxiviteRAGAdapter.ts` — bridges RAG pipeline
- `arxivite/src/lib/super-chat/ArxivitePersistenceAdapter.ts` — persistence bridge
- `arxivite/src/lib/super-chat/setupAdapters.ts` — adapter setup
- `arxivite/src/lib/super-chat/index.ts` — exports

## Progress
1. ⬜ Change `useSuperChat` default from `false` to `true`
2. ⬜ Remove legacy path from ChatbotAssistant.tsx
3. ⬜ Test with real tools
4. ⬜ Polish error handling and streaming

## Dependencies
- **T20** (CRITICAL): Real-time streaming must be fixed first, or arxivite's super-chat path will have broken UX
- None otherwise — arxivite's super-chat integration already exists

## Impact
- **Instantly**: True multi-step tool calling (already works, just not default)
- **Instantly**: Native streaming (already works, just not default)
- **Instantly**: More reliable tool execution (native vs simulated)
- **Foundation**: For T19 (full arxivite integration) — which is now mostly polish

## Notes
- This is still the **fastest win** — but the effort is much smaller than originally thought
- **IMPORTANT**: Must wait for T20 (streaming fix) or users will see no progress during tool execution
- The original estimate ("delete shim, rewrite adapter") was based on a false premise
- Actual work: ~1-2 days, not "rewrite everything"
- After this fix, arxivite's RAG system (which is genuinely good) will work with proper tool calling by default

