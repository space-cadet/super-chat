# T21: npm Release & GitHub CI/CD

*Created: 2026-06-20 12:10:00 IST*

**Status**: 🔄 **IN PROGRESS**
**Priority**: HIGH
**Phase**: Publishing Infrastructure

## Overview

Set up the complete publishing pipeline: LICENSE, GitHub Actions CI/CD, comprehensive README, and npm publish configuration.

## Completed

### 1. LICENSE ✅
- **File**: `LICENSE`
- **Type**: MIT License
- **Copyright**: 2026 Deepak Vaid

### 2. GitHub Actions Workflow ✅
- **File**: `.github/workflows/build-release.yml`
- **Jobs**:
  - `build-and-test`: Typecheck → Test → Build → Upload artifacts (on every push/PR)
  - `release`: Create GitHub Release with dist/, README, LICENSE (on tag `v*`)
  - `publish-npm`: Publish to npm registry (on tag `v*`)

### 3. README ✅
- **File**: `README.md`
- **Sections**: Features, Quick Start (core/tools/React/hooks), Architecture, Adapters, Multi-Agent, Stream Events, Tool Approval, Development, Testing, Browser Support, License

### 4. package.json fixes ✅
- `prepublishOnly: "npx tsup"` — Works in CI without pnpm
- Added keywords, author, repository fields

## Still Needed

- [ ] Configure `NPM_TOKEN` secret in GitHub repository settings
- [ ] Verify GitHub Actions runs successfully on next push
- [ ] Test release creation by pushing a tag
- [ ] Verify npm publish works end-to-end

## How to Publish

```bash
# 1. Set git identity (stops the warning)
git config user.name "Deepak Vaid"
git config user.email "your@email.com"

# 2. Bump version
npm version minor  # or patch, major

# 3. Push tag (triggers CI)
git push origin main --tags
```

CI will automatically:
1. Run tests
2. Build package
3. Create GitHub Release
4. Publish to npm

## Files Created/Modified

| File | Action | Purpose |
|------|--------|---------|
| `LICENSE` | Created | MIT License |
| `.github/workflows/build-release.yml` | Created | CI/CD pipeline |
| `README.md` | Updated | Complete documentation |
| `package.json` | Updated | prepublishOnly, metadata |

## Dependencies
- npm account for `space-cadet`
- GitHub repository with Actions enabled
- `NPM_TOKEN` secret configured in repo settings

## Notes
- Package name `super-chat` is available on npm (verified via dry-run)
- React components are included in `/react` subpath export
- Build auto-runs before publish via `prepublishOnly`
- Version currently 0.1.0 — consider bumping to 0.2.0 for first real release
