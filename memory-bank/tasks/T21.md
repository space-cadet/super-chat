# T21: npm Release & GitHub CI/CD

*Created: 2026-06-20 12:10:00 IST*
*Updated: 2026-06-20 16:38:00 IST*

**Status**: 🔄 **IN PROGRESS**
**Priority**: HIGH
**Phase**: Publishing Infrastructure

## Overview

Set up the complete publishing pipeline: LICENSE, GitHub Actions CI/CD, comprehensive README, and npm publish configuration.

## Completed

### 1. LICENSE ✅
- **File**: `LICENSE`
- **Type**: MIT License
- **Copyright**: 2026 Deepak Vaid

### 2. GitHub Actions Workflow ✅
- **File**: `.github/workflows/build-release.yml`
- **Jobs**:
  - `build-and-test`: Typecheck → Test → Build → Upload artifacts (on every push/PR)
  - `release`: Create GitHub Release with dist/, README, LICENSE (on tag `v*`)
  - `publish-npm`: Publish to npm registry (on tag `v*`)

### 3. README ✅
- **File**: `README.md`
- **Sections**: Features, Quick Start, Architecture, Adapters, Multi-Agent, Stream Events, Tool Approval, Development, Testing

### 4. package.json fixes ✅
- `prepublishOnly: "npx tsup"` — Works in CI without pnpm
- Added keywords, author, repository fields

### 5. CI Fix ✅ (2026-06-20 15:30)
- **Problem**: `pnpm-workspace.yaml` had invalid content causing `ERROR: packages field missing or empty`
- **Fixes**:
  - Deleted `pnpm-workspace.yaml` (single package repo, not monorepo)
  - Added `pnpm-workspace.yaml` to `.gitignore` (pnpm v11+ auto-recreates)
  - Removed `demo/**/*` from `tsconfig.json` (TS6059 outside rootDir)
  - Fixed unused `USER_ID` import in `Orchestrator.test.ts`
  - Created `.npmrc` with `auto-install-peers=true`
  - Added `pnpm.onlyBuiltDependencies: ["esbuild"]` to `package.json`
- **Result**: Build & Test passes (run ID 27867800049, 27s)

### 6. Full-Featured Demo ✅ (2026-06-20 16:15)
- **Features**:
  - Provider selector (DeepSeek, Kimi, OpenRouter, Gemini)
  - Mock mode for UI testing without API keys
  - Connection test for each provider
  - Demo scenarios (Calculate, Weather, arXiv, Web Search)
  - Visual tool flow (pending → approve → result)
- **Files**:
  - `demo/.env.local` — API keys for 4 providers
  - `demo/vite.config.ts` — Vite `define` block to inject env vars
  - `demo/main.tsx` — Full demo with MockLLMAdapter
- **Reference research**: Examined obsidian-ai's `settings.ts` and `api.ts` for:
  - `ProviderType` union, `ProviderProfile` interface
  - `getDefaultModel()`, `getDefaultEndpoint()`, `getProviderColor()` helpers
  - `createLanguageModel()` factory with per-provider SDK initialization
- **Key technical fix**: Vite aliases changed from `../dist` to `../src` to avoid React Refresh HMR injection into pre-built bundles (root cause of blank page)

## Still Needed

- [ ] Configure `NPM_TOKEN` secret in GitHub repository settings
- [ ] Push version tag to test CI end-to-end
- [ ] Verify npm publish works end-to-end

## How to Publish

```bash
# 1. Set git identity (stops the warning)
git config user.name "Deepak Vaid"
git config user.email "your@email.com"

# 2. Bump version
npm version minor  # or patch, major

# 3. Push tag (triggers CI)
git push origin main --tags
```

CI will automatically:
1. Run tests
2. Build package
3. Create GitHub Release
4. Publish to npm

## API Keys Configured

| Provider | Key | Status |
|----------|-----|--------|
| DeepSeek | `sk-d8a2...27d5` | ✅ Active |
| Kimi | `sk-TDNz...Mfw` | ✅ Active |
| OpenRouter | `sk-or-v1...881a` | ✅ Active |
| Gemini | `AIzaSy...Rinw` | ✅ Active |

**Source**: Original `API_Keys` file from `typora-notes` (shared via Telegram on June 15, 2026).

## Files Created/Modified

| File | Action | Purpose |
|------|--------|---------|
| `LICENSE` | Created | MIT License |
| `.github/workflows/build-release.yml` | Created | CI/CD pipeline |
| `README.md` | Updated | Complete documentation |
| `package.json` | Updated | prepublishOnly, metadata |
| `pnpm-workspace.yaml` | Deleted | Invalid content |
| `.gitignore` | Updated | Ignore pnpm-workspace.yaml |
| `tsconfig.json` | Modified | Removed demo/**/* |
| `.npmrc` | Created | auto-install-peers |
| `demo/.env.local` | Created | API keys for demo |
| `demo/vite.config.ts` | Modified | Env var injection, fixed aliases |
| `demo/main.tsx` | Modified | Full demo with MockLLMAdapter |

## Dependencies
- npm account for `space-cadet`
- GitHub repository with Actions enabled
- `NPM_TOKEN` secret configured in repo settings

## Notes
- Package name `super-chat` is available on npm (verified via dry-run)
- React components are included in `/react` subpath export
- Build auto-runs before publish via `prepublishOnly`
- Version currently 0.1.0 — consider bumping to 0.2.0 for first real release
