# T15: Port obsidian-ai mature agent logic into super-chat

*Created: 2026-06-20 00:10:00 IST*
*Last Updated: 2026-06-20 00:10:00 IST*

**Status**: ⬜ **PENDING**
**Priority**: HIGH
**Phase**: Phase 1 — Build the Unified Core

## Context
obsidian-ai has the most mature agent logic but is tightly coupled to Obsidian. We need to port the generic parts into super-chat's engine layer.

## What to Port

### 1. `formatToolResult()` — EXTRACTED to T21
- **Status**: Now tracked as **T21** (separate high-priority task)
- See `tasks/T21.md` for full specification

### 2. `AgentLoop` enhancements (high priority)
- Source: `obsidian-ai/src/agent/AgentLoop.ts`
- Target: `super-chat/src/core/AgentLoop.ts` (replace or extend)
- What to port:
  - `maxSteps` multi-step iteration with proper error handling
  - `requestApproval` / `autoApply` pattern
  - `maxRetries` for failed tool calls
  - Better event flow: `tool-call`, `tool-result`, `tool-approval-requested`, `stream-complete`
  - **CRITICAL**: Must yield events in real-time (see T20)

### 3. `Orchestrator.ts` — Multi-Agent (medium priority) — EXTRACTED to T6
- **Status**: Now tracked as **T6** (existing task)
- Source: `obsidian-ai/src/agent/Orchestrator.ts`
- Target: `super-chat/src/agent/Orchestrator.ts`
- Keep: Sequential mode, debate mode, full/isolated context, profile resolution
- Make generic: Replace Obsidian profile types with generic `AgentProfile`
- **Note**: Group chat with tools does NOT support manual approval — returns error asking to enable auto-approve

### 4. `MentionParser.ts` (medium priority) — EXTRACTED to T7
- **Status**: Now tracked as **T7** (existing task)
- Source: `obsidian-ai/src/agent/MentionParser.ts`
- Target: `super-chat/src/agent/MentionParser.ts`
- Parse `@AgentName` mentions in user messages

### 5. Token estimation + context truncation (low priority) — EXTRACTED to T23
- **Status**: Now tracked as **T23**
- Source: `obsidian-ai/src/agent/AgentLoop.ts` (stepTokenEstimates)
- Target: `super-chat/src/core/AgentLoop.ts`

### 6. Reasoning-delta support (medium priority)
- Source: `obsidian-ai/src/agent/AgentLoop.ts`
- obsidian-ai's AgentLoop supports reasoning tokens but does NOT include them in assistant messages (Vercel SDK strips them)
- Ensure super-chat's AgentLoop handles reasoning-delta events correctly

## Progress
1. ⬜ Port `formatToolResult()` — **Now T21**
2. ⬜ Fix AgentLoop real-time streaming — **Now T20** (must be done first!)
3. ⬜ Enhance `AgentLoop` with obsidian-ai's patterns (error handling, retries, approval)
4. ⬜ Port `Orchestrator.ts` — **Now T6**
5. ⬜ Port `MentionParser.ts` — **Now T7**
6. ⬜ Port token estimation — **Now T23**
7. ⬜ Add reasoning-delta support
8. ⬜ Test all patterns in demo app

## Dependencies
- **T20** (CRITICAL): Real-time streaming must work before any AgentLoop enhancements matter
- **T21**: formatToolResult is now a separate prerequisite
- T14 (UI components should be ready for AgentLoop testing)
- T3/T4 (super-chat's existing AgentLoop + ToolExecutor)

## Notes
- The `formatToolResult()` function is the fastest win — but now tracked as T21
- **CRITICAL**: T20 (streaming fix) blocks ALL tool-enabled UX. Without it, users see nothing during multi-step execution.
- `AgentLoop` in obsidian-ai uses `streamText({ tools, stopWhen: stepCountIs(1) })` — same pattern as super-chat but with more mature error handling
- Keep all Obsidian-specific code OUT of super-chat — use adapter interfaces only
- **Known issue**: Group chat with tools in obsidian-ai does NOT support manual approval — it returns an error message
