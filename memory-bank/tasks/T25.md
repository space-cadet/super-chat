# T25: Shared Agentic Tool Runtime and Built-in Provider

*Created: 2026-09-06 13:13:22 IST*
*Last Updated: 2026-09-06 20:27:32 IST*

**Status**: ⬜ **PENDING**
**Priority**: CRITICAL
**Phase**: Shared Runtime
**Parent**: T15, T22

Supporting document:
[`implementation-details/agentic-tool-runtime.md`](../implementation-details/agentic-tool-runtime.md).

## Objective

Extract the mature obsidian-ai agentic tool-calling behavior into the
host-agnostic super-chat runtime and provide the initial built-in tool pack.

## Subtasks

- [ ] **T25a** — Inventory and define the 31 built-in tool schemas.
- [x] **T25b** — Implement composed tool-provider registration and resolution.
- [ ] **T25c** — Implement shared execution, approval, cancellation, retry, and audit behavior.
- [ ] **T25d** — Implement structured tool results and evidence-producing tools.
- [ ] **T25e** — Add runtime, provider, and fixture-host acceptance tests.

## Ownership

`super-chat` owns the runtime, safety, lifecycle, and result contracts.
Hosts supply handlers for tools that depend on product data or platform APIs.

## 2026-09-06 Minimal Provider Seam

Completed the smallest provider-composition slice after a read-only survey of
`obsidian-ai`'s canonical registry, executor, turn coordinator, model-history,
and tool tests. `super-chat` now accepts one or several host tool capabilities,
resolves each tool name to its owning capability, rejects duplicate names, and
passes the active abort signal through adapter execution and the shared agent
loop. Existing single-provider adapters remain source-compatible.

This does not define or copy the 31 built-in Obsidian tools, and it does not
claim the full T25 runtime. Approval, cancellation, and existing execution
behavior remain in the current engine while the later extraction adds
structured results, evidence, retry, and audit behavior.

Verification: TypeScript passed; 16 Vitest files / 145 tests passed; the
package ESM/CJS/declaration build passed; the Arxivite engine harness passed 3
tests; and `git diff --check` passed. The full Arxivite readiness suite remains
blocked by unrelated pre-existing changes in the external Arxivite checkout.

## Dependencies

- T15 agentic behavior extraction.
- T22 host contracts and fixture host.

## Completion Criteria

- The built-in provider exposes the approved 31-tool catalog.
- Multiple host providers can be composed without shared-core product imports.
- Tool calls stream, pause for approval, cancel, retry, and fail safely.
- Structured results can contribute evidence and citations.
- Fixture, Obsidian, and Arxivite provider contracts pass.
