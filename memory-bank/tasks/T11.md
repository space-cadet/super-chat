# T11: Build System & npm Publish

*Created: 2026-05-19 14:10:00 IST*
*Last Updated: 2026-05-19 14:56:00 IST*

**Status**: ✅ **COMPLETE**
**Priority**: HIGH

## Details

Set up proper build pipeline to produce distributable output. Currently `package.json` has a placeholder build script. Need actual dual-format (ESM + CJS) output with TypeScript declarations.

## Progress
1. ✅ Configure tsup for dual ESM/CJS build
2. ✅ Verify `dist/` output structure
3. ✅ Test imports in both ESM and CJS contexts
4. ⬜ Add `prepublishOnly` script
5. ⬜ Publish to npm (or set up git install)

## Files
- `tsup.config.ts` — Build configuration
- `package.json` — Updated build scripts

## Build Output
```
dist/
├── index.js          (ESM, 35KB)
├── index.cjs         (CJS, 40KB)
├── index.d.ts        (TypeScript declarations, 16KB)
├── index.js.map      (Source map)
└── react/
    ├── index.js      (ESM — placeholder for T12)
    ├── index.cjs     (CJS)
    └── index.d.ts    (TypeScript declarations)
```

## Notes
- Must output both `.js` (ESM) and `.cjs` (CJS)
- Must generate `.d.ts` declaration files
- Must handle peer dependencies correctly (react optional)
- Must tree-shake unused providers (already lazy-loaded)

## Acceptance Criteria
- [x] `pnpm build` produces `dist/index.js`, `dist/index.cjs`, `dist/index.d.ts`
- [x] React subpath exports work: `import { useChat } from '@space-cadet/super-chat/react'`
- [ ] Can be installed via `npm install` or `npm install github:space-cadet/super-chat`

## Next Steps
- T12: React Hooks — useChat, useAgent
- Add `prepublishOnly` script when ready to publish
- Consider git-install workflow for now: `npm install github:space-cadet/super-chat`