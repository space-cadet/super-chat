# T16: Port arxivite RAG pipeline as RAGAdapter

*Created: 2026-06-20 00:10:00 IST*
*Last Updated: 2026-06-20 00:10:00 IST*

**Status**: ⬜ **PENDING**
**Priority**: MEDIUM
**Phase**: Phase 1 — Build the Unified Core

## Context
arxivite has the best RAG pipeline but it's embedded in the app. We need to extract the generic RAG logic into a reusable `RAGAdapter` that implements super-chat's `RAGAdapter` interface.

## Source
- `arxivite/src/lib/rag/chatbotRAGFlow.ts`
- `arxivite/src/lib/rag/ChatbotIntentRouter.ts`
- `arxivite/src/lib/rag/ChatbotIntentRouter.ts` (intent classification: 8 types)

## Target
- `super-chat/src/adapters/RAGAdapter.ts` (interface already exists)
- `super-chat/src/adapters/ArxiviteRAGAdapter.ts` (reference implementation)

## Interface (already exists in super-chat)
```typescript
interface RAGAdapter {
  retrieveContext(query: string): Promise<ContextItem[]>;
}
```

## What to Port
1. **Intent classification** — 8 intent types from arxivite's router
2. **PocketFlow pipeline** — the RAG retrieval pipeline
3. **Anti-hallucination prompt** — system prompt that prevents made-up citations
4. **Paper filtering** — domain-specific filtering for arxivite

## Progress
1. ⬜ Audit arxivite's RAG files for generic vs. specific code
2. ⬜ Extract generic RAG logic into `RAGAdapter` interface implementations
3. ⬜ Create `ArxiviteRAGAdapter` with arxivite-specific features
4. ⬜ Test with demo app

## Dependencies
- T5 (Adapter interfaces already exist in super-chat)

## Notes
- arxivite's RAG is heavily tied to its PDF chunk database — the generic adapter should accept any vector store
- The `ArxiviteRAGAdapter` can be a reference implementation that shows how to wire domain-specific RAG
- Keep the intent router — it's genuinely useful for routing queries to the right tool
